{
  "name": "@exocognito/angelmcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "angel": "cd examples && pnpm exec angel",
    "test": "bun test tests/cloud",
    "test:golden": "bun run scripts/golden.ts",
    "test:google-read-proof": "bun run scripts/google-read-proof.ts",
    "typecheck": "tsc --noEmit",
    "typecheck:workers": "bun run tsc -p tsconfig.broker.json --noEmit && bun run tsc -p tsconfig.gateway.json --noEmit && bun run tsc -p tsconfig.control.json --noEmit",
    "check:core": "pnpm --dir packages/core run check",
    "check:ws1": "bun run scripts/ws1-release-integrity.ts",
    "check": "bun run typecheck && bun run typecheck:workers && bun test tests/cloud && pnpm run check:core && bun run scripts/check-fixture-staleness.ts && bun run scripts/build-demo-fixture.ts .demo-fixture.check.json && rm -f .demo-fixture.check.json && pnpm run check:ws1 && pnpm run check:cli && pnpm run check:ws2cli",
    "check:ws2cli": "bun run scripts/ws2-cli-install-proof.ts",
    "check:cli": "pnpm --dir packages/cli run check"
  },
  "dependencies": {
    "@smcllns/angel-core": "workspace:0.3.0",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260711.1",
    "@types/bun": "^1.1.3",
    "typescript": "^5.4.5",
    "wrangler": "^4.110.0"
  }
}
