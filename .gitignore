node_modules/
.secrets/
.env
*.local
.DS_Store

# personal scratch pad, local only
docs/sam-feedback.md

# actual deployment configuration, local only
angels/*/angel.json

# agent tooling session transcripts, local only
.pi/

# transient output of `bun run check` (see scripts/build-demo-fixture.ts)
.demo-fixture.check.json
